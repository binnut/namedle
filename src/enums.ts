export enum GameMode {
	"daily",
	"hourly",
	"infinite",
	// "minutely",
	"name",
	"middle_name1",
	"middle_name2"
};

export const enum ms {
	SECOND = 1000,
	MINUTE = 1000 * 60,
	HOUR = 1000 * 60 * 60,
	DAY = 1000 * 60 * 60 * 24,
	YEAR = 1000 * 60 * 60 * 24 * 365
};